version: '3.8'
services:
  mysql-db:
    image: mysql:8.0
    container_name: turnos_mysql
    environment:
      MYSQL_ROOT_PASSWORD: rootpassword
      MYSQL_DATABASE: turnos_db
    ports:
      - "3306:3306"